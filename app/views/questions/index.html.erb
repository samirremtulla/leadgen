<h1>All Questions</h1>

<table class="table-striped">
  <thead>
    <th>Question</th>
  </thead>
    <tr>
      <% @questions.each do |question|%>
      <td><%= question.description %> </td>
      <td><%= link_to "More Details!", question_path(question)%>
    </tr>
    <%end%>
</table>